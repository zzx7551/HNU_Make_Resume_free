# 简历制作 1.0 · 技术文档

> 面向"想看懂这个项目怎么写、顺便学一下 Python Web 框架"的读者。
> 全文按「**为什么这么设计 → 代码在哪 → 关键几行怎么写的**」组织，所有代码片段都来自本仓库真实代码。

---

## 0. 一分钟看懂

### 0.1 它是什么

一个**本地跑的单机 Web 应用**：双击 `启动.bat` → 起一个本地 HTTP 服务 → 浏览器打开 → 左边编辑、右边 A4 实时预览 → 一键导出 PDF / 长图。

### 0.2 架构图

~~~text
┌──────────────────────────── 浏览器（前端 3 个文件） ────────────────────────────┐
│  index.html          编辑器外壳（顶栏 / 左侧面板 / 右侧预览 / 弹窗）             │
│  js/app.js           编辑器逻辑：状态管理、增删改、撤销、自动保存、裁剪、导出调用  │
│  js/render.js        渲染器：把 JSON 数据 → 渲染成简历 DOM（预览和导出共用）      │
│  resume.css          简历模板样式（预览和导出共用）                              │
│  editor.css          编辑器自己的样式（跟简历无关）                              │
└───────────────┬──────────────────────────────────────────────┬────────────────┘
                │ fetch /api/*  (JSON)                         │ /export/view/{id}
                ▼                                              ▼
┌─────────────────────────── FastAPI 后端（app/ 5 个文件） ───────────────────────┐
│  main.py      路由 + 静态文件 + 启动（uvicorn）                                  │
│  store.py     JSON 文件存储（简历 / 头像 / 备份），无数据库                       │
│  sample.py    默认模板数据 + 版式参数（"内容"层面的东西）                          │
│  exporter.py  导出编排：起无头浏览器 → 打开导出页 → 截图 / 打印成 PDF             │
│  cdp.py       极简 Chrome DevTools Protocol 客户端（websocket + JSON-RPC）       │
└───────────────┬──────────────────────────────────────────────┬────────────────┘
                │ 读写                                          │ 启动 & 驱动
                ▼                                              ▼
        data/、exports/ 文件                     msedge.exe --headless=new（无头浏览器）
~~~

### 0.3 技术栈

| 层 | 用什么 | 说明 |
|---|---|---|
| 语言 | Python 3.12（conda 环境） | 后端 |
| Web 框架 | **FastAPI** + **uvicorn** | 路由、请求解析、静态文件 |
| 浏览器驱动 | **websocket-client** + 手写 CDP 客户端 | 驱动本机 Edge/Chrome 无头模式 |
| 图像处理 | **Pillow** | 只用于导出 PNG 的落盘优化 |
| 上传 | **python-multipart** | FastAPI 处理 `multipart/form-data` 需要它 |
| 前端 | 原生 HTML + CSS + ES5 风格 JavaScript | 无框架、无打包、无 npm |
| 数据 | 本地 JSON 文件 | 无数据库 |

**依赖只有 5 个**，见 `requirements.txt`。

### 0.4 一次「编辑」的时序

~~~text
用户敲字
  └─ 预览里 contenteditable 触发 input 事件
       └─ app.js 用 data-path 定位，「ResumeRenderer.setByPath」改内存里的 App.resume
            └─ markDirty() → 1.2 秒防抖后 save()
                 └─ PUT /api/resume/{id}  →  main.py:api_put  →  store.save()
                      └─ 覆盖写 data/resumes/{id}.json（写之前先备份一份到 data/backups/）
~~~

### 0.5 一次「导出 PDF」的时序

~~~text
点「导出 PDF」
  └─ 先 save()，保证磁盘上的 JSON 是最新的
       └─ POST /api/export/{id} {"format":"pdf"}
            └─ exporter.export_pdf()
                 ├─ 起/复用无头浏览器（第一次约 1~2 秒）
                 ├─ 新开标签页 → 打开 http://127.0.0.1:8765/export/view/{id}
                 │    （这个页面加载的是同一套 resume.css + render.js！）
                 ├─ 等页面里的 window.__READY__ === true（导出页自己打的标记）
                 ├─ Emulation.setEmulatedMedia(media=print)
                 ├─ Page.printToPDF(preferCSSPageSize=true)
                 └─ 落盘 exports/时间戳_简历名.pdf → 返回下载地址
  └─ 前端把 URL 塞进 <a download> 点一下 → 浏览器下载
~~~

---

## 1. 技术选型：为什么是这些

**为什么用 Python + FastAPI，而不是 Java/Spring 或 Flask/Django？**

- **FastAPI 的学习曲线最平缓**：一个函数 + 一个装饰器就是一个接口，类型注解直接变成参数校验和文档。
  自带交互式文档 `http://127.0.0.1:8765/api/docs`，不用写 Swagger。
- **导出这件事必须调本机浏览器**，Python 里调子进程 + 长连接 websocket 很轻；换成 Java 要拖一堆依赖。
- **Flask 也能写**，但 FastAPI 原生支持 async、自动校验、自动文档，代码量更少；
  Django 太重（ORM/模板/Admin 这个项目一个都用不上）。
- **单文件 JSON 存储**：这是个人工具，不需要数据库。用户随时能打开 `data/resumes/xxx.json` 看、改、备份。

**为什么前端不用 Vue/React？**

- 页面结构简单（一个编辑器 + 一个预览），没有复杂组件树、没有路由；
- **零构建**：不用 Node/npm/webpack，改完 CSS 刷新就能看到，项目的可维护性反而更高；
- 关键点：**预览和导出必须用同一套渲染代码**，用原生 JS 写一个 `render.js` 最直接，
  打包工具反而会引入"开发态/生产态两套产物"的风险。

**为什么导出要用无头浏览器？**

这是整个项目最核心的决定。要"导出和预览一模一样"，只有一条路：**让同一个渲染引擎渲染同一份 HTML/CSS**。
自己用 Pillow/ReportLab 从头画，就得把 CSS 的换行、字距、行高全部重写一遍，两边永远对不齐。
所以：预览用 Chromium 渲染，导出也用 Chromium（无头模式）渲染 **同一个页面**，只是"拍照"和"打印"的区别。

---

## 2. 后端：FastAPI 是怎么写的

### 2.1 先补 8 个 FastAPI 基本概念（对照本项目）

~~~python
from fastapi import FastAPI, Body, File, HTTPException, Query, UploadFile
from fastapi.responses import HTMLResponse, FileResponse, RedirectResponse
from fastapi.staticfiles import StaticFiles

app = FastAPI(title="简历制作 1.0", docs_url="/api/docs")   # ① 应用对象
~~~

| # | 概念 | 本项目里的例子 | 说明 |
|---|---|---|---|
| ① | `FastAPI()` | `app = FastAPI(...)` | 应用实例，uvicorn 启动的就是它 |
| ② | 路由装饰器 | `@app.get("/api/resumes")` | 一个函数 = 一个接口；路径里的 `{rid}` 会传给同名参数 |
| ③ | 类型注解即校验 | `def api_export(rid: str, ...)` | 注解 `int` 就自动转成整数，转不了自动返回 422 |
| ④ | 请求体 | `payload: dict = Body(...)` | POST/PUT 的 JSON body；`...` 表示必填，`default={}` 表示可省略 |
| ⑤ | 查询参数 | `print: int = Query(0)` | URL 里 `?print=1` |
| ⑥ | 文件上传 | `file: UploadFile = File(...)` | 需配合 `python-multipart`；`await file.read()` 拿字节 |
| ⑦ | 响应类型 | `response_class=HTMLResponse` / `FileResponse` / `RedirectResponse` | 不写就默认把返回值序列化成 JSON |
| ⑧ | 静态文件 | `app.mount("/static", StaticFiles(directory=...))` | 一个目录挂到一个 URL 前缀下 |

### 2.1.1 同步 `def` 和异步 `async def` 到底差在哪（最容易写错的地方）

**先记住一个画面：uvicorn 默认只开「一个服务员」。**

你写的所有接口代码，默认都跑在**同一个事件循环（event loop）**里，相当于餐厅只有 1 个服务员。
区别只在于：**这个服务员遇到"需要等"的事情时，是站着干等，还是先去招呼别人。**

| 写法 | 服务员的行为 | 结果 |
|---|---|---|
| `async def` 里 `await` 一个**真异步**操作 | 把菜交给厨房，**转身去招呼下一桌**，好了再回来取 | 1 个服务员能同时应付很多桌 ✅ |
| `async def` 里写**阻塞**代码（`time.sleep`、`requests.get`、`subprocess`、大循环计算…） | **站在厨房门口死等**，谁叫都不理 | 整个服务被拖成串行，页面转圈 ❌ |
| 普通 `def`（同步） | FastAPI 知道你会阻塞，**自动再叫一个临时工**（丢进线程池执行） | 也不会卡，但线程有开销、数量有上限（默认 40）✅ |

> 一句话口诀：**`async def` 里的每一行，都必须是"不会让 CPU 干等"的代码。**
> 拿不准就写普通的 `def` —— FastAPI 会自动把它放进线程池，这是最安全的默认选择。

**这不是纸上谈兵，跑一下 `examples/sync_vs_async.py` 就能看到**（三个接口内部都耗时 1 秒，同时发 5 个请求）：

~~~text
/sync        并发 5 个 -> 总耗时 1.02s    各自耗时: 1.01 1.01 1.02 1.02 1.02    <- 同步 def：5 个并行
/async-ok    并发 5 个 -> 总耗时 1.03s    各自耗时: 1.03 1.03 1.03 1.03 1.03    <- async + await：5 个并行
/async-bad   并发 5 个 -> 总耗时 5.02s    各自耗时: 1.01 2.01 3.01 4.01 5.01    <- async 里写阻塞：排队！
~~~

第三个接口就是典型的"事故写法"：它明明是 `async def`，但里面写了 `time.sleep(1)`，
于是**第一个请求占住事件循环的那 1 秒里，后面 4 个请求谁也进不来**，只能一个接一个排队。

**那么本项目是怎么选的？**

~~~python
# ① 上传接口：异步。里面只有 await file.read()，Starlette 内部就是丢线程池实现的，真的不阻塞循环
@app.post("/api/upload")
async def api_upload(file: UploadFile = File(...)):
    content = await file.read()
    ...

# ② 导出接口：同步。里面全是"阻塞操作"，写成 async 会把整个网站卡住
@app.post("/api/export/{rid}")
def api_export(rid: str, payload: dict = Body(default={})):
    ...   # 内部有 subprocess.Popen 起浏览器、websocket.recv() 等浏览器回包、Pillow 存图
~~~

试想如果导出写成 `async def`：一次导出要跑 1~2 秒，这 1~2 秒里
**你连左侧编辑器的自动保存请求都发不出去**（它排在同一个事件循环后面），页面直接卡住。
写成同步 `def` 之后，导出在后台线程里慢慢跑，页面照样能编辑、能保存。

**判断方法：问自己"这行代码会不会让 CPU 干等？"**

| 会干等（阻塞）→ 用同步 `def` | 不会干等（可以 await）→ 可以用 `async def` |
|---|---|
| `time.sleep()` | `await asyncio.sleep()` |
| `requests.get()`（同步 HTTP 库） | `await httpx.AsyncClient().get()` |
| `subprocess.run()` / `websocket.recv()` | `await asyncio.create_subprocess_exec()` |
| 同步数据库驱动（pymysql / sqlite3） | 异步驱动（asyncpg / aiosqlite） |
| 普通文件读写、Pillow 图片处理 | `await file.read()`（Starlette 已帮你丢线程池） |

**如果非要在 `async def` 里做阻塞的事**，用一行把它"挪走"：

~~~python
from fastapi.concurrency import run_in_threadpool
# 或者：import asyncio;  await asyncio.to_thread(func, arg)

@app.get("/api/something")
async def something():
    result = await run_in_threadpool(blocking_func, arg1, arg2)   # 丢到线程池，不阻塞事件循环
    return result
~~~

**四个常见坑**（新手几乎都会踩）：

1. ❌ `async def` + `requests.get()` → 全站卡死。要么换 `httpx`，要么把接口改成同步 `def`。
2. ❌ `async def` + `time.sleep()` → 同上，改成 `await asyncio.sleep()`。
3. ❌ `async def` + 几十万次的循环计算 → 同上，用 `run_in_threadpool` 或改成同步 `def`。
4. ❌ 以为"加了 async 就变快" → `async` 只是"等待时不占着位置"，**它不会让 CPU 算得更快**，
   计算密集型的活儿该用多进程还是得用多进程。

### 2.2 五个后端文件各管什么

| 文件 | 行数 | 职责 | 一句话 |
|---|---|---|---|
| `app/main.py` | 208 | 路由、静态文件、启动 | 唯一的"入口层" |
| `app/store.py` | 119 | 文件存储、备份、上传落盘 | 数据层，不依赖 FastAPI |
| `app/sample.py` | 162 | 默认模板数据 + 版式参数 | 纯数据，没有逻辑 |
| `app/exporter.py` | 120 | 导出编排（PNG / PDF / HTML） | 业务层 |
| `app/cdp.py` | 242 | 无头浏览器 + CDP 协议 | 基础设施层，可独立复用 |

**分层原则**：只有 `main.py` 认识 FastAPI；`store.py`、`cdp.py` 拿到别的地方也能用。
这样你以后想换成 Flask、或者写个命令行批量导出脚本，都不用重写数据层。

### 2.3 关键代码逐段讲

#### ① 启动：为什么有个 `if __package__ in (None, "")`

~~~python
if __package__ in (None, ""):          # 说明是 python app/main.py 直接跑的
    sys.path.insert(0, str(Path(__file__).resolve().parent.parent))
    from app.exporter import Exporter
else:                                   # 说明是被 import 的（uvicorn app.main:app）
    from .exporter import Exporter
~~~

Python 的包机制：以「包」导入时用相对导入 `from .exporter`，直接当脚本跑时相对导入会失败。
这两行让**两种启动方式都能用**。日常启动走的是 `run.py → app.main:run()`，属于后者。

#### ② 端口占用自动避让

~~~python
def pick_port(preferred: int = 8765) -> int:
    for port in [preferred, 8766, 8767, 8768, 8899, 0]:   # 0 = 让系统随便分一个
        try:
            s = socket.socket()
            s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
            s.bind(("127.0.0.1", port))
            got = s.getsockname()[1]
            s.close()
            return got
        except OSError:
            continue
~~~

"先 bind 一下看能不能占上"是探测端口是否被占用的标准手法。注意**服务只监听 127.0.0.1**，
不对外网开放，这也是这个工具"数据不出本机"的前提。

#### ③ 导出接口：为什么先 `store.get(rid)`

~~~python
@app.post("/api/export/{rid}")
def api_export(rid: str, payload: dict = Body(default={})):
    fmt = (payload.get("format") or "pdf").lower()
    try:
        store.get(rid)                     # 先确认简历存在，不存在直接 404
    except FileNotFoundError:
        raise HTTPException(404, "简历不存在")
    try:
        if fmt == "png":
            info = exporter.export_png(rid, scale=float(payload.get("scale") or 2))
            return {"ok": True, "format": "png", "url": "/exports/" + info["filename"], ...}
    except Exception as exc:
        raise HTTPException(500, "导出失败：%s" % exc)
~~~

三个细节：
1. **参数校验前置**：不存在就 404，不要等到浏览器里报一堆错；
2. **`HTTPException` 是把异常翻译成 HTTP 状态码的正确姿势**，FastAPI 会自动序列化成 `{"detail": "..."}`，
   前端 `app.js` 里的 `api()` 就是读这个 `detail` 弹提示的；
3. **最外层 catch 住所有异常**：导出链路长（子进程 + 网络 + 文件），失败原因必须原样回给前端。

#### ④ 存储：为什么每次保存前先备份

~~~python
def save(self, rid: str, data: dict) -> dict:
    path = self._path(rid)
    if path.exists():
        shutil.copyfile(path, BACKUP_DIR / ("%s.%s.bak" % (rid, time.strftime("%Y%m%d%H%M%S"))))
    data = dict(data or {})
    data["id"] = rid
    data["updatedAt"] = _now()
    path.write_text(json.dumps(data, ensure_ascii=False, indent=2), encoding="utf-8")
    return data
~~~

- `ensure_ascii=False` 才能把中文原样写进 JSON（否则是 `\u5f20\u4e09` 这种转义，人能看不能读）；
- `indent=2` 让 JSON 文件**人能直接看懂/手改**；
- 备份文件按时间戳命名，误删可以翻 `data/backups/` 找回来。

还有一处安全细节：

~~~python
SAFE_ID = re.compile(r"^[A-Za-z0-9_-]{1,64}$")

def _path(self, rid: str) -> Path:
    if not SAFE_ID.match(rid or ""):
        raise ValueError("非法的简历 ID")
    return RESUME_DIR / ("%s.json" % rid)
~~~

因为 `rid` 来自 URL，如果直接拼路径，别人请求 `/api/resume/..%2F..%2Fwindows%2Fsystem32` 就可能写到别处去。
**白名单正则**是最简单可靠的防路径穿越手段。

#### ⑤ 导出编排：一把锁 + 复用浏览器

~~~python
class Exporter:
    """线程安全：所有导出串行执行，复用同一个浏览器进程。"""

    def __init__(self, base_url: str):
        self.base_url = base_url.rstrip("/")
        self._browser = None
        self._lock = threading.Lock()

    def export_png(self, rid: str, scale: float = 2.0) -> dict:
        with self._lock:                       # ← 同一时刻只允许一个导出在跑
            page = None
            try:
                page = self._open(rid)         # 打开导出页并等它就绪
                rect = self._paper_rect(page)  # 量出"纸张"的位置和尺寸
                png = page.screenshot_png(clip=rect, scale=scale)
            finally:
                if page is not None:
                    page.close()               # 用完关标签页，但浏览器留着复用
~~~

- FastAPI 把同步接口丢线程池 → **多个导出可能并发**，而浏览器只有一个，所以用 `threading.Lock` 串行化；
- 浏览器进程启动一次（1~2 秒）后一直复用，第二次导出就很快了；
- 等待就绪用的是「页面打标记 + 主机轮询」：

~~~python
def _open(self, rid: str, media: str = "screen"):
    page = self._browser_instance().new_page()
    page.set_viewport(900, 1400, 1.0)
    if media == "print":
        page.send("Emulation.setEmulatedMedia", {"media": "print"})
    page.navigate("%s/export/view/%s" % (self.base_url, rid))
    page.wait_for("window.__READY__ === true", timeout=60)             # 数据渲染完
    page.eval("document.fonts && document.fonts.ready ? document.fonts.ready.then(()=>1) : 1",
              await_promise=True)                                       # 字体加载完
    page.wait_for("window.__LAYOUT_STABLE__ === true", timeout=30)      # 图片加载完、两帧之后
    return page
~~~

#### ⑥ `cdp.py`：60 行核心，手写一个浏览器驱动

Chrome DevTools Protocol 本质就是 **websocket 上的 JSON-RPC**：
发 `{"id":1,"method":"Page.printToPDF","params":{...}}`，回 `{"id":1,"result":{...}}`。

~~~python
class Page:
    def send(self, method: str, params=None) -> dict:
        with self._lock:
            self._id += 1
            mid = self._id
            self._ws.send(json.dumps({"id": mid, "method": method, "params": params or {}}))
            while True:
                raw = self._ws.recv()
                msg = json.loads(raw)
                if msg.get("id") == mid:            # 跳过事件通知，只认自己那条响应
                    if "error" in msg:
                        raise CDPError(...)
                    return msg.get("result", {})
~~~

**为什么要自己写而不是装 playwright/selenium？**
1. 依赖更少（只要一个 `websocket-client`，几十 KB）；
2. 直接用你**本机已有的 Edge**，不用再下一份 150MB 的 Chromium；
3. 核心就 `send` 一个方法加几个封装，看懂了对理解 CDP 帮助很大。

启动参数里几个值得注意的：

~~~python
args = [
    self.exe, "--headless=new",     # 新版无头模式（和真实浏览器渲染一致）
    "--disable-gpu", "--hide-scrollbars",
    "--remote-allow-origins=*",     # 新版本 CDP 的 Origin 校验（不加会连不上）
    "--force-device-scale-factor=1",
    "--disable-lcd-text",           # 关掉亚像素抗锯齿，导出的图更干净
    "--allow-file-access-from-files",
    "--remote-debugging-port=%d" % self.port,
    "--user-data-dir=%s" % self._tmp,
]
creation = 0x08000000 if os.name == "nt" else 0    # CREATE_NO_WINDOW：不弹黑框
~~~

### 2.4 后端 API 一览

| 方法 | 路径 | 作用 | 请求 | 返回 |
|---|---|---|---|---|
| GET | `/` | 编辑器页面 | — | HTML |
| GET | `/export/view/{rid}` | 导出专用页（无头浏览器加载的就是它） | — | HTML |
| GET | `/print/{rid}` | 跳转到导出页并自动唤起打印 | — | 302 |
| GET | `/api/resumes` | 简历列表 | — | `{"items":[{id,name,updatedAt}]}` |
| GET | `/api/resume/{rid}` | 读一份简历 | — | 简历 JSON |
| PUT | `/api/resume/{rid}` | 保存（覆盖） | 简历 JSON | 保存后的 JSON |
| POST | `/api/resume` | 新建 | `{"name":..,"mode":"blank"\|"sample"}` | 新简历 JSON |
| POST | `/api/resume/{rid}/duplicate` | 复制一份 | — | 新简历 JSON |
| DELETE | `/api/resume/{rid}` | 删除 | — | `{"ok":true}` |
| GET | `/api/default` | 取（没有就创建）默认简历 | — | `{id,resume}` |
| POST | `/api/upload` | 上传头像（multipart） | form-data 的 file | `{"url":"/data/uploads/xx.jpg"}` |
| POST | `/api/export/{rid}` | 导出 | `{"format":"png"\|"pdf"\|"html","scale":2}` | `{ok,url,filename,...}` |
| GET | `/api/health` | 自检（浏览器路径、端口） | — | `{ok,browser,baseUrl}` |
| GET | `/api/docs` | FastAPI 自动生成的交互式文档 | — | Swagger UI |

> 静态资源：`/static/*` → `web/` 目录；`/data/*` → `data/`（头像）；`/exports/*` → `exports/`（导出成品）。

---

## 3. 前端：不用框架，怎么把一个编辑器写清楚

### 3.1 三个文件的分工

~~~text
index.html    只负责"骨架 + 壳"：顶栏按钮、左侧三个面板容器、右侧预览容器、几个弹窗
editor.css    编辑器界面样式（跟简历长相无关）
resume.css    ★ 简历模板样式（预览和导出共用，只认 CSS 变量）
js/render.js  ★ 渲染器：JSON → 简历 DOM（预览和导出共用）
js/app.js     编辑器：状态、交互、侧栏表单、撤销、保存、裁剪、导出
~~~

画 ★ 的两个文件是「**同一份代码被两个页面用**」：编辑器页面和导出页面都 `<script src="/static/js/render.js">`，
所以**预览长什么样，导出就一定长什么样**。

### 3.2 渲染器 `render.js`：数据 → DOM

#### ① 一切靠 `data-path` 寻址

渲染时给每个可编辑文本打上它在数据里的"路径"：

~~~javascript
function editable(node, opts, path, placeholder) {
  node.setAttribute('data-path', path);           // 例如 "sections.0.items.1.title"
  if (opts.editable !== false) {                  // 导出页传 editable:false，就不加 contenteditable
    node.setAttribute('contenteditable', 'true');
    if (placeholder) node.setAttribute('data-ph', placeholder);
  }
  return node;
}
~~~

路径的读写就两个小函数（等价于 lodash 的 get/set）：

~~~javascript
ResumeRenderer.getByPath(resume, "sections.0.items.1.title")      // 读
ResumeRenderer.setByPath(resume, "sections.0.items.1.title", "x") // 写
~~~

**这么做的好处**：编辑器完全不需要知道 DOM 结构对应哪个字段，只要 `e.target.closest('[data-path]')` 拿到路径即可，
渲染层和编辑层彻底解耦。这也是"点击预览直接改字"能几行实现的原因。

#### ② 主题 → CSS 变量

版式参数（字号、行高、边距…）全部是 CSS 变量，JS 只做一次映射：

~~~javascript
var THEME_MAP = { fontSize: v => v + 'px', fontFamily: v => '"' + v + '", ... };
var VAR_MAP   = { fontSize: '--rc-body-size', padX: '--rc-pad-x', ... };

function applyTheme(paper, theme) {
  Object.keys(THEME_MAP).forEach(function (key) {
    paper.style.setProperty(VAR_MAP[key], THEME_MAP[key](theme[key]));
  });
}
~~~

于是「版式样式」面板里改一个数字 → 改 CSS 变量 → 整个预览立刻变，**不需要重新渲染 DOM**。

#### ③ 空内容处理（导出时自动省略）

~~~javascript
function isBlankItem(item) {                 // 这条内容是不是空的？
  if (item.type === 'blank') return false;   // 空行是有意义的，不算空
  if (item.type === 'entry') { ... }         // 条目：标题栏和子内容都空才算空
  ...
}
function markBlank(node, opts, item) {
  if (node && opts.editable === false && isBlankItem(item)) node.classList.add('is-empty');
  return node;
}
~~~
配合 CSS：`.paper:not(.is-editable) .is-empty { display: none !important; }`
——**编辑时看得见（可填），导出时自动消失（不留空行）**。

### 3.3 编辑器 `app.js`

#### ① 只有一个状态对象

~~~javascript
var App = {
  rid: null, resume: null,      // ★ 唯一数据源：整个简历就是这一个对象
  zoom: 1, autoFit: true, showGuides: true,
  dirty: false, undoStack: [], redoStack: [], selectedSection: null, list: []
};
~~~

所有界面都是它的"投影"：`renderPreview()`（预览）、`renderSidebar()`（左侧表单）、`renderTopbar()`（顶栏）。
这就是 React 的核心思想，只是这里用函数手写了一遍。

#### ② 三种改数据的方式，各有讲究

| 场景 | 做法 | 为什么不统一重渲染 |
|---|---|---|
| 在**左侧表单**里输入 | 改 state → `renderPreview()` | 只刷预览，**左侧表单不重绘 → 光标不丢** |
| 在**预览里就地编辑** | 改 state，**完全不重渲染** | 重渲染会把光标顶掉，等 `focusout` 再统一重绘 |
| **增删改/换位** | 改 state → `renderAll()` | 结构变了，两边都要重画 |

~~~javascript
// 预览内的输入：只更新数据，不动 DOM
paper.addEventListener('input', function (e) {
  var node = e.target.closest('[data-path]');
  if (!node) return;
  setByPath(node.getAttribute('data-path'), node.textContent);
  markDirty();
});

// 离开输入框时再统一重绘（保证标记/样式回到正常状态）
paper.addEventListener('focusout', function (e) { ... renderPreview(); renderSidebar(); });
~~~

#### ③ 事件委托：一份监听管住整棵树

预览和侧栏的 DOM 是**动态重建**的，如果每次重建都重新绑事件，代码会失控。
所以只在外层容器上绑一次，靠 `closest()` 找到真正触发元素：

~~~javascript
sidebar.addEventListener('click', function (e) {
  var btn = e.target.closest('[data-act]');       // 按钮都带 data-act
  if (!btn) return;
  var act = btn.getAttribute('data-act');
  if (act === 'add-entry') addItem(btn.getAttribute('data-path'), 'entry');
  else if (act === 'item-del') removeItem(...);
  ...
});
~~~

#### ④ 防抖自动保存 + 撤销栈

~~~javascript
var saveSoon = debounce(function () { save(); }, 1200);   // 停手 1.2 秒才写盘

function pushUndo() {                                     // 结构性操作前先压栈
  App.undoStack.push(clone(App.resume));
  if (App.undoStack.length > 80) App.undoStack.shift();
  App.redoStack.length = 0;
}
function undo() {
  if (!App.undoStack.length) return;
  App.redoStack.push(clone(App.resume));
  App.resume = App.undoStack.pop();
  renderAll(); markDirty();
}
~~~
「深拷贝快照」是最朴素也最不容易错的撤销实现（简历数据才几十 KB，完全够用）。

#### ⑤ 侧栏是"拼字符串"拼出来的

~~~javascript
function itemEditor(item, path) {
  if (item.type === 'entry') {
    out.push('<div class="grid2">');
    out.push('<input type="text" data-bind="' + path + '.time" value="' + esc(item.time) + '">');
    ...
  }
}
~~~
统一约定：**输入框带 `data-bind="路径"`，点一下带 `data-act="动作"`**，
于是"输入"和"点击"两个委托监听就能覆盖整个侧栏。注意所有插值都过 `esc()` 转义，防止内容里的 `<` 破坏 HTML。

#### ⑥ 头像裁剪：canvas 的坐标换算

核心就一次比例换算 + 一次 `drawImage`：

~~~javascript
function confirmCrop() {
  var s = Crop.sel, k = Crop.natW / Crop.dispW;     // 显示尺寸 → 原图尺寸的比例
  var sx = Math.round(s.x * k), sy = Math.round(s.y * k);
  var sw = Math.round(s.w * k), sh = Math.round(s.h * k);
  var outW = Math.round(theme.photoWidth * 2);      // 输出头像的 2 倍图，保证清晰
  var canvas = document.createElement('canvas');
  canvas.width = outW;  canvas.height = Math.round(outW * sh / sw);
  canvas.getContext('2d').drawImage(Crop.img, sx, sy, sw, sh, 0, 0, canvas.width, canvas.height);
  canvas.toBlob(function (blob) { uploadAvatarBlob(blob); }, 'image/jpeg', 0.93);
}
~~~
拖动手柄时按「对角固定 + 比例约束」重算矩形，见 `cropResize()`。

### 3.4 样式：两套 CSS，互不干扰

- `editor.css`：只作用于编辑器界面（面板、按钮、弹窗），用 `.panel / .section-card / .crop-box` 之类的类名；
- `resume.css`：只作用于 `.paper` 里面，**全部尺寸走 CSS 变量**，前缀统一 `r-`（resume 的意思）。

这样「改编辑器界面」永远影响不到「导出的简历长相」。

---

## 4. 导出原理（本项目最核心的部分）

### 4.1 三条链路

| 格式 | 手段 | 结果 |
|---|---|---|
| **PDF** | CDP `Page.printToPDF` + CSS `@page` | A4 分页、**矢量文字**（放大不糊）、字体内嵌 |
| **PNG** | CDP `Page.captureScreenshot` + `captureBeyondViewport` | **一整张长图**，多页内容天然合并、没有分页缝 |
| **HTML** | 直接抓 `document.documentElement.outerHTML` | 单文件，能离线打开/打印 |

### 4.2 长图是怎么"多页合成一张"的

不用拼接！`captureBeyondViewport: true` 让 Chromium **一次性把整篇文档（不管多长）渲染并截下来**，
所以"超过两页自动合并成一页"是天然的，不存在页缝、不存在缩放不一致。

~~~python
def screenshot_png(self, clip=None, scale=2.0) -> bytes:
    params = {"format": "png", "captureBeyondViewport": True, "fromSurface": True}
    if clip:
        params["clip"] = dict(clip, scale=scale)      # clip 决定"截哪块"，scale 决定清晰度
    return base64.b64decode(self.send("Page.captureScreenshot", params)["data"])
~~~

截取范围用 `getBoundingClientRect()` 从页面里量出来（只截「纸张」区域，不截编辑器界面）：

~~~javascript
{ x: r.left + window.scrollX, y: r.top + window.scrollY, width: r.width, height: r.height }
~~~

还有个兜底：文档特别长时自动降倍率，避免生成超大图（`MAX_PNG_HEIGHT = 30000`）。

### 4.3 PDF 的 A4 与分页

导出页会根据主题参数动态注入 `@page`（把 px 换算成 mm，让**打印边距 = 预览内边距**）：

~~~javascript
function px2mm(px) { return (px * 25.4 / 96).toFixed(3) + 'mm'; }
style.textContent = '@page { size: A4; margin: ' + px2mm(t.padY || 28) + ' ' + px2mm(t.padX || 52) + '; }';
~~~

`resume.css` 里再控制分页行为：

~~~css
@media print {
  .paper { width: auto !important; padding: 0 !important; }   /* 纸张留白交给 @page */
  .r-section-title, .r-rule { break-after: avoid; }           /* 标题不孤零零留在页尾 */
  .r-entry, .r-item, .r-header { break-inside: avoid; }       /* 条目不被拦腰截断 */
}
~~~

### 4.4 踩过的坑（都已在代码里规避）

| 坑 | 现象 | 解决 |
|---|---|---|
| 字体没加载完就截图 | 文字变成默认字体/缺字 | 等 `document.fonts.ready` + 两帧 `requestAnimationFrame` |
| 图片没加载完 | 头像位置空白 | 导出页统计未完成的 `img`，全部 load/error 后再置 `__READY__` |
| CSS 缓存 | 改了样式导出没变 | 浏览器每次新开页面，靠 FastAPI 的 ETag 正常回源 |
| 分页把条目切两半 | PDF 里一条经历被拆到两页 | `break-inside: avoid` |
| 大图内存爆 | 超长简历截图失败 | 按高度自动降倍率，并给出提示 |

---

## 5. 数据模型

一份简历就是**一个 JSON 对象**（存在 `data/resumes/{id}.json`）：

~~~javascript
{
  "id": "ae028128338a",
  "name": "我的简历",                    // 简历名（下拉框里显示）
  "updatedAt": "2026-09-17 11:01:37",
  "basics": {                            // ① 头部
    "name": "张三",
    "photo": "/data/sample/avatar-default.png",   // 空字符串 = 不显示头像
    "showPhoto": true,
    "info": [ { "label": "手机", "value": "13800000000", "visible": true }, ... ]
  },
  "sections": [                          // ② 模块（有序数组 = 顺序）
    {
      "id": "edu", "title": "教育经历", "visible": true,
      "items": [                         // ③ 内容块（有序数组）
        {
          "type": "entry",               // 内容块类型
          "time": "2021.09-2025.06", "title": "某某大学",
          "mid": "本科", "right": "计算机科学与技术（国家级一流本科专业建设点）",
          "details": [ { "type": "text", "label": "成绩排名", "value": "..." } ]
        }
      ]
    }
  ],
  "theme": { "fontSize": 13.5, "lineHeight": 21, "padX": 52, ... }   // ④ 版式（CSS 变量）
}
~~~

**四种内容块**（`render.js:renderItem`）：

| type | 渲染成 | 字段 |
|---|---|---|
| `entry` | 一行四栏 + 下方子内容 | `time / title / mid / right / details[]` |
| `text` | 「标签：内容」整行 | `label / value` |
| `list` | 带标记的一行 | `marker`（`"auto"` 自动编号 1. 2. / `"+"` / `"•"` / `""`）、`value` |
| `blank` | 一个空行 | — |

**`details` 可以再嵌套 `entry`**，所以模块结构是递归的（当前 UI 只用到一层，数据模型是支持的）。

---

## 6. 版式是怎么"抠"出来的（可复用的方法论）

需求是「跟成品图一模一样」，做法不是目测，而是**把图当数据来量**：

1. **量页面骨架**：扫描整图找「横线」（整行都是深色的行）→ 得到 5 条分隔线位置；
   扫描墨迹的最左/最右 → 得到左右边距 83/1196（1280 宽图）。
2. **定倍率**：内容宽 1113px ÷ 推测的设计稿 800px ≈ **1.6 倍**（同时验证了行距 33.5 ÷ 1.6 = 21px 是整数）。
3. **量字号**：统计每条文字带的**墨迹高度/宽度**，与本地渲染的候选字号对比 → 正文 13.5px、姓名 28px、标题 17px。
4. **判粗细**：用**墨迹密度**（深色像素占外框比例）区分字重——
   正文 0.29 = 常规，模块标题 0.56、条目名称/中栏/职位 0.46~0.55 = **加粗**。
5. **验证**：用同一份内容渲染出来，再量一遍行带位置，逐项对齐（本项目最终误差在 1~3 个 CSS 像素内）。

这套方法可以直接复用到"仿任何版式"的场景。

---

## 7. 动手改一改（三个典型需求）

### 7.1 加一种内容块类型（例如「时间轴」）

1. **渲染**：`render.js` 的 `renderItem()` 里加一个分支：
   ~~~javascript
   if (type === 'timeline') { /* 造 DOM，字段用 editable(node, opts, path + '.value') */ }
   ~~~
2. **空值判断**：`isBlankItem()` 里补一条规则（否则导出时会留空行）；
3. **新建默认值**：`app.js` 的 `newItem(type)` 加一个分支，侧栏的「+ 条目」按钮加 `data-act="add-timeline"`，
   再到 `bindSidebar()` 里加一行 `else if (act === 'add-timeline') addItem(path, 'timeline')`；
4. **样式**：`resume.css` 里加 `.r-timeline { ... }`。

—— 4 个文件各加几行就完成，这就是"渲染器 + 路径寻址"带来的扩展性。

### 7.2 加一个导出格式（例如 Word/docx 或 Markdown）

1. 后端写一个 `export_md(rid)`：读 `store.get(rid)` 的 JSON，按结构拼 Markdown 文本；
2. `main.py:api_export` 里加一个 `if fmt == "md": ...` 分支，把文件写进 `exports/` 并返回 URL；
3. `app.js` 顶栏加个按钮 → `doExport('md')`。

（若想做 docx，用 `python-docx` 即可，但注意：**这就无法保证"和预览一模一样"了**，
所以本项目只保留了浏览器渲染这一条链路。）

### 7.3 改版式 / 换一套模板

- **微调**：直接在「版式样式」面板改，值会存进 `theme`；
- **改默认值**：改 `app/sample.py:default_theme()` 与 `app.js:DEFAULT_THEME()`（两处保持一致）；
- **换模板**：改 `resume.css` 里 `.paper` 的变量默认值和结构样式；
  想加"模板选择"，就是准备几组 theme 预设，切换时把 `resume.theme` 换掉再 `renderAll()`。

---

## 8. 调试与测试

~~~bat
:: 1) 看接口是否活着 & 无头浏览器能不能找到
curl http://127.0.0.1:8765/api/health

:: 2) 直接看"导出页"长什么样（浏览器打开，和导出结果完全一致）
http://127.0.0.1:8765/export/view/{简历id}

:: 3) 用接口导出（PowerShell）
Invoke-RestMethod -Method Post -Uri http://127.0.0.1:8765/api/export/{id} ^
  -ContentType 'application/json' -Body '{"format":"png","scale":2}'

:: 4) 交互式 API 文档（FastAPI 自带，能直接点着试）
http://127.0.0.1:8765/api/docs
~~~

| 现象 | 排查方向 |
|---|---|
| 导出报"未找到 Edge/Chrome" | 装 Edge，或设环境变量 `RESUME_BROWSER` 指向浏览器 exe |
| 导出很慢 | 首次要起浏览器（1~2 秒）；之后会复用 |
| 导出的图和预览不一致 | 通常是字体缺失；看「版式样式 → 字体」是否为本机已装的字体 |
| 端口被占 | 启动时会自动换到 8766/8767/8768/8899 |
| 中文乱码 | 所有文件读写都显式 `encoding="utf-8"`；Windows 控制台显示乱码不影响文件内容 |

---

## 9. 顺带学：Python 工程写法小结

| 点 | 本项目做法 | 说明 |
|---|---|---|
| 包结构 | `app/` 包 + `run.py` 入口 | 入口和实现分离，方便以后加 CLI |
| 导入 | 包内相对导入 `from .store import Store` | 配合 `__package__` 判断兼容"直接运行" |
| 类型注解 | `def get(self, rid: str) -> dict:` | 既是文档也是 IDE 提示，FastAPI 还拿它做校验 |
| 文档字符串 | 每个模块/类一句中文说明 | `"""线程安全：所有导出串行执行…"""` |
| 路径 | `pathlib.Path` 而不是拼字符串 | `ROOT / "data" / "resumes"` 跨平台 |
| 异常 | 自定义 `CDPError`，业务层统一转 `HTTPException` | 异常分层清晰 |
| 常量 | 大写 + 模块级：`SAFE_ID`、`MAX_PNG_HEIGHT` | |
| 依赖 | `requirements.txt` + `pip install -r` | 少于 10 个依赖，无锁定文件也够用 |
| 字符串格式化 | 用 `%` 而非 f-string | 为了兼容老版本解释器；新项目用 f-string 更好 |

**推荐的阅读顺序**（想快速建立全局观）：
`run.py` → `app/main.py`（看有哪些接口）→ `web/index.html`（看页面骨架）→
`web/js/render.js`（看数据怎么变 DOM）→ `web/js/app.js`（看交互）→ `app/exporter.py` + `app/cdp.py`（看导出黑魔法）。

---

## 附录：文件清单

| 文件 | 行数 | 说明 |
|---|---|---|
| `run.py` | 11 | 启动入口 |
| `启动.bat` | 19 | 双击启动（检查依赖 + 起服务 + 开浏览器） |
| `app/main.py` | 208 | FastAPI 路由与启动 |
| `app/store.py` | 119 | JSON 存储 / 备份 / 上传 |
| `app/sample.py` | 162 | 默认模板数据与版式参数 |
| `app/exporter.py` | 120 | 导出编排（PNG / PDF / HTML） |
| `app/cdp.py` | 242 | 无头浏览器 + CDP 客户端 |
| `web/index.html` | 145 | 编辑器骨架 |
| `web/editor.css` | 229 | 编辑器样式 |
| `web/resume.css` | 149 | 简历模板样式（★ 预览导出共用） |
| `web/js/render.js` | 302 | 渲染器（★ 预览导出共用） |
| `web/js/app.js` | 1102 | 编辑器逻辑 |
| `web/export.html` | 64 | 导出专用页 |
| `技术文档.md` | 660+ | 本文档 |
| `examples/sync_vs_async.py` | 100 | 可运行的小实验：同步 def / async / async 里写阻塞 的耗时对比 |

> 数据目录（不进 Git）：`data/resumes/` 简历、`data/uploads/` 头像、`data/backups/` 自动备份、`exports/` 导出成品。
